@symbolic $a : array<int>, $i : int;
a = $a;
@assume a.size() == 8;
a.pushBack($i);
@assert $a.size() + 1 == a.size();
@assert a[2] == $a[2];
n = 3;
while (n > 0)
{
    a.pushBack(n);
    n--;
}
@assert $a.size() + 4 == a.size();
@assert a[a.size() - 1] == 1;