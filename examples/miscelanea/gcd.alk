/*
  gcd
  alki gcd.alk 
  alki gcd.alk gcd.in
  where the gcd.in includes:
u |-> 42 v |-> 56
*/
gcd(a, b)
{
  while (a != b) {
    if (a > b)  a = a - b;
    if (b > a) b = b - a;
  }
  return a;
}

//x = gcd(12, 8);
//x = gcd(u, v);
print(gcd(u,v));



