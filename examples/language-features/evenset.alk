/*
  alki evenset.alk evenset.in
  where, e.g., evenset.in includes
  a |-> {1, 2, 3, 4}
*/

// s = emptySet;
s = { 0 };
forall x in a   // a is a input set
  if (x % 2 == 0) 
    s = s U singletonSet(x);
print(s);
