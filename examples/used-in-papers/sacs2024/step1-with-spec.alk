/*
base(out n)
@requires n: int
@requires n == 3
@ensures n == 0
*/
{
    // the first turn
    // choose take from {1,2};
    //  ... symbolically
    n=3;
    @havoc take: int;
    @assume 1 <= take && take <= 2;
    n = n - take;
    // the next turn
    take = n % 3;
    n = n - take;
}