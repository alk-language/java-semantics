@havoc n: int;
@assume n % 3 == 0; 
// the first turn 
// choose take from {1,2};
//  ... symbolically
@havoc take: int;
@assume 1 <= take && take <= 2;
n = n - take;
// the next turn
take = n % 3;
n = n - take;
@assert n % 3 == 0;