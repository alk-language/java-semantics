#include<cmmdc.alk>

f(x)
{
    return x;
}

@requires forall i : int => 0 <= i && i < a. size() − 1 ==> a[i] <= a[i+1]
@ensures  forall i : int => 0 <= i && i < a. size() − 1 ==> a[i] <= a[i+1]

slp = 1;
i = 1;
while ( i < a.size())
{
    if (a [i] == a [i − slp])
        slp = slp +1;
    i = i + 1;
}