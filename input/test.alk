min(a,  b)
@requires a : int && b : int
@requires a >= 0 && b >= 0
@ensures \result : int
@ensures \result >= 0
@ensures \result <= \old(a) && \result <= \old(b)
{
    if (a < b) {
        return a;
    } else {
        return b;
    }
}