a = $a;
// env: a |-> $a
// pc:  true
while (a > 0)
{
    a--;
    b++;
}

CFG.Node(while)

ProgramContext = <Environment, PathCondition>

Lattice<ProgramContext, <)


Dataflow:
// env: a |-> a($w), b |-> b($w)
// pc:  !(a($w) > 0)




Executie simbolica + Solver:
Formula de recurenta:
a(0) = $a
a(t) = a(t-1) - 1
a(t-1) > 0
------------------------ Solver
$w = $a, a($w) = 0


// env: a |-> 0, b |-> b($w)
// pc:  !(a($w) > 0)