symbolic $a : array<int>;
a = $a;

assume a.size() == 5;

maxim = a[0];
for (i = 1; i < a.size(); i++)
// invariant forall j s.t. 0 <= j < i . a[j] <= maxim
{
    if (maxim < a[i])
        maxim = a[i];
}

print(maxim);