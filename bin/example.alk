gcd(a, b)
{
    while (b > 0)
    {
        r = a % b;
        a = b;
        b = r;
    }

    return a;
}
	
print(gcd(a, b));

print({<1, 2>, <1, 2>});
print({} == {});

x = "ana.are.mere";
print(x.split());
print(x.split("\."));
